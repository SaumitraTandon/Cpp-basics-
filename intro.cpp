#include<iostream> // iostream is kind of a file that contain the code that defines cout
using namespace std; // We want to use cout of the std name face

int main() {
    
    cout << "Namaste Duniya" << endl; // << is to write ; endl is end line ; ; is like punctuation for the compiler
    int a = 123;
    cout << a << endl;

    int size = sizeof(a);
    cout << "Size of a is : "<< size << endl;
    cout << "My enemies are many, my equals are none." << endl;
}